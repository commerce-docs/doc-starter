{
  "name": "storefront-components",
  "repository": {
    "type": "git",
    "url": "https://github.com/magento-ux/storefront-components"
  },
  "scripts": {
    "start": "jekyll serve --watch",
    "build": "jekyll build --config _config.yml,_config.prod.yml",
    "deploy": "npm run build && gh-pages -d _site"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
